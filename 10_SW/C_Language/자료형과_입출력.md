---
tags:
  - C언어
---
## 자료형

| 자료형 | 크기 | 설명 | 예시 |
|--------|------|------|------|
| `int` | 4바이트 | 정수 | `int a = 10;` |
| `float` | 4바이트 | 실수 (소수점 6자리) | `float b = 3.14;` |
| `double` | 8바이트 | 실수 (소수점 15자리) | `double c = 3.14159;` |
| `char` | 1바이트 | 문자 1개 | `char d = 'A';` |

## 입출력

```c
// 출력
printf("%d\n", a);    // 정수
printf("%.2f\n", b);  // 실수 소수점 2자리
printf("%c\n", d);    // 문자
printf("%s\n", str);  // 문자열

// 입력 (&를 빠뜨리면 안 됨!)
int x;
scanf("%d", &x);
```

## 형식 지정자

| 지정자 | 타입 |
|--------|------|
| `%d` | int |
| `%f` | float |
| `%lf` | double |
| `%c` | char |
| `%s` | 문자열 |

## Python vs C 핵심 차이

| 항목 | Python | C |
|------|--------|---|
| 변수 선언 | `x = 10` | `int x = 10;` |
| 정수 나눗셈 | `10 / 3 = 3.333...` | `10 / 3 = 3` (정수!) |
| 논리 연산자 | `and`, `or`, `not` | `&&`, `\|\|`, `!` |
| 블록 구분 | 들여쓰기 | `{ }` |

## 주의사항
- C에서 `int / int = int` → 소수점 버려짐
- 실수 결과 필요하면 `(double)sum / n` 으로 형변환

## 관련 노트
- [[형변환]]
- [[배열_입력패턴]]
